//in progress
#include<bits/stdc++.h>
#include <numeric>
using namespace std;

int LIST[5]={0},LINK[5]={3,4,0,1,-1},START = 2,AVAIL =2;

  void  INSERT(int loc,int item)
  {
    if(AVAIL == -1)
    {
        cout<<"Overflow\n";
        return;
    }
      int NEW = AVAIL;
      AVAIL= LINK[AVAIL];
      LIST[NEW] = item;
  if(LIST[loc] == 0)
  {
    //LINK[NEW] = START;START = NEW;
    
    return;
  }

  //AVAIL = LINK[AVAIL];
  int a = LINK[NEW],b=LINK[loc];
  if(a == b)
  return;
  LINK[NEW] = LINK[loc];
  LINK[loc] = NEW;
  
  }
  void show()
  {
    int ptr = START;
  
    for(int i = 0;i<=7;i++)
    {
         if(LIST[ptr] == 0)
         break;
        cout<<LIST[ptr]<<endl;
        // cout<<"ptr = "<< ptr<<" avail = "<<AVAIL<<endl;
             if( LINK[ptr] == -1 ) break;
        ptr= LINK[ptr];
      
    }
  }

int main()
{
   
      INSERT(2,1);
       show();
      
      cout<<endl<<endl;
      INSERT(2,2);
      INSERT(2,3);
      INSERT(2,4);
      INSERT(3,5);
      INSERT(2,4);//Overflow
      
    show();
   
}
